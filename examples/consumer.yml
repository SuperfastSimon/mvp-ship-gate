name: mvp-ship-gate

on:
  pull_request:
  push:
    branches: [main, master]
    tags: ["v*"]
  workflow_dispatch:
    inputs:
      mode:
        description: light or full
        type: choice
        options: [light, full]
        default: full

concurrency:
  group: mvp-ship-gate-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  gate:
    name: mvp-ship-gate
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v5
      - name: Decide mode
        id: mode
        run: |
          if [ "${{ github.event_name }}" = "workflow_dispatch" ]; then
            echo "value=${{ github.event.inputs.mode }}" >> "$GITHUB_OUTPUT"
          elif [ "${{ github.event_name }}" = "pull_request" ]; then
            echo "value=light" >> "$GITHUB_OUTPUT"
          else
            echo "value=full" >> "$GITHUB_OUTPUT"
          fi
      - name: Run mvp-ship-gate
        # Pin to SHA until a release tag exists. Do not use @main in production.
        uses: SuperfastSimon/mvp-ship-gate@c81a1ab97d8cefa260cbf61db72331b83b4f2ca4
        with:
          root: .
          mode: ${{ steps.mode.outputs.value }}
          format: github
